
    /*
    bullishSignal: `${stock} has increased to a price of ${currentPrice}
        Bullish signal`
   
        alert = `${stock} has hit target price of ${currentPrice}
        Bullish signal`
   
        alert = `${stock} has dropped to price of ${currentPrice}
        Bearish signal`
        break;
  
        alert = `${stock} has dropped to price of ${currentPrice}
        Bearish signal warning`
        break;
  
        alert = `${stock} has hit target price of ${currentPrice}
        Bearish signal`
}*/